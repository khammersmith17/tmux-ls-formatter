Simple script to read in the output from tmux ls command and get some nice formatting. I used tmux ls often to see my running tmux session, so just a small little personal terminal utility.

If you would like to use this tool, you can clone the repo and cargo install from local.
